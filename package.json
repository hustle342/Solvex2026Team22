{
  "name": "recruitai-monorepo",
  "version": "0.2.0",
  "private": true,
  "description": "RecruitAI frontend testing setup for Sprint 2",
  "scripts": {
    "test": "jest --coverage --runInBand",
    "test:ci": "jest --coverage --runInBand",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  }
}
